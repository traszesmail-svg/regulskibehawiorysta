# Plan 1307 — SEO, oferta, lejek i porządki repozytorium

Data rozpoczęcia: 2026-07-13
Repozytorium: `C:\projekt\regulskibehawiorysta`

## Zasady zakresu

- Najpierw realizujemy dokładnie punkty 1–5 z audytu.
- W komunikacji oferty używamy określenia **połączenie telefoniczne**. Nie używamy nazwy operatora.
- Kwadrans i krótkie formaty nie wymagają eksponowania numeru telefonu; klient podaje dane w formularzu, a właściciel uzgadnia termin.
- Pełna konsultacja pozostaje przez Jitsi z pokojem klienta przez 14 dni.
- Quiz i lejek będą osobnym etapem: mają dawać wiedzę, bezpiecznie porządkować sytuację i dopiero potem proponować zakres rozmowy.
- Nie wykonujemy deployu, dopóki testy, QA i porządkowanie repozytorium nie będą zakończone.

## Checklist

- [x] Utworzyć plan 1307 i zabezpieczyć stan repozytorium.
- [x] 1. Ujednolicić ceny w UI, API, checkout, FAQ, metadata i schema.
- [x] 2. Naprawić schema opinii i przygotować walidację w Search Console.
- [x] 3. Usunąć wewnętrzne linki do adresów przekierowywanych.
- [x] 4. Ujednolicić komunikację: połączenie telefoniczne / Jitsi / pokój 14 dni / SLA.
- [x] 5. Ustabilizować daty modyfikacji w sitemapie.
- [x] 6. Przebudować quiz i lejek merytorycznie oraz konwersyjnie.
- [x] 7. Uporządkować repozytorium, typecheck, testy i Visual QA.
- [x] 8. Wykonać commit, deploy i kontrolę produkcyjną.

## Dziennik postępu

### 2026-07-13 — start

- Audyt wykonany na bieżącym working tree.
- Search Console: własność zweryfikowana, strona główna możliwa do indeksowania, sitemap przyjęta, 4 błędne elementy opinii.
- Repozytorium miało niezacommitowane zmiany; nie są usuwane bez osobnej decyzji.
- Zidentyfikowano rozjazd cen 74/104/174/475 kontra stare 69/99/169/470.
- Rozpoczęto realizację punktów 1–5. Quiz, lejek, obrazy i pełne porządki pozostają poza zakresem tej tury.

### 2026-07-13 — wykonanie punktów 1–5

- Ceny usług są pobierane z `PUBLIC_SERVICE_PRICE_AMOUNTS` w `lib/funnel.ts`: 74 / 104 / 174 / 475 zł. UI, API, checkout, FAQ, metadata, quiz i JSON-LD korzystają z tych samych etykiet.
- Schema opinii została ograniczona do `CollectionPage` + `ItemList` na stronie opinii. Usunięto globalne i zduplikowane `Review` / `AggregateRating`, które powodowały 4 błędne elementy w Search Console. Po ponownym crawlu trzeba potwierdzić wynik w GSC.
- Dodano `lib/public-routes.ts`; publiczne linki omijają stare ścieżki `/psy`, `/koty` i legacy landingi. Legacy pozostają tylko jako kompatybilne wejścia/redirecty. Z sitemap usunięto legacy landingi.
- Oferta mówi o połączeniu telefonicznym dla krótkich formatów i o Jitsi dla pełnej konsultacji. Nazwa operatora nie występuje w ofercie. Pełna konsultacja zachowuje pokój klienta przez 14 dni.
- Sitemap ma stabilną datę zastępczą i daty publikacji wpisów blogowych; nie generuje `lastModified` z bieżącego czasu przy każdym żądaniu. `/termin` ma `noindex`, bo jest technicznym widokiem pośrednim.
- Walidacja: `npm.cmd run lint` — OK; `npm.cmd run schema-audit` — OK; `npm.cmd run build` — OK, 92/92 stron statycznych. `npm.cmd test` — 76 zaliczonych, 8 niezaliczonych, 13 pominiętych: 4 błędy `EPERM` tworzenia sandboxu danych oraz 4 asercje testów utrzymujących stare copy/schema/ścieżki. Nie są to błędy kompilacji.
- Deploy nie został wykonany. Punkty 6–8 pozostają do wznowienia: quiz/lejek, sprzątanie repozytorium + Visual QA, a dopiero potem commit i deploy.

### 2026-07-14 — domknięcie realnych braków po audycie

- Quiz nie sprzedaje usługi przed ukończeniem bezpiecznej ścieżki. Wynik najpierw pokazuje dopasowany artykuł i stronę problemu, a dopiero potem proponuje zakres rozmowy; zachowano twarde bramki bezpieczeństwa i zdrowia.
- Linki edukacyjne z warstwy wzrostu i wpisów blogowych prowadzą do konkretnych artykułów zamiast do ogólnego `/blog`. Obrazy poniżej pierwszego ekranu mają lazy loading. Lokalny audyt Stage 9: 10 kontroli, 0 błędów, 5 ostrzeżeń overflow do ręcznego przeglądu.
- Ceny publiczne pozostają scentralizowane: 74 / 104 / 174 / 475 zł. UI pokazuje etykietę „Cena promocyjna” i tygodniową ważność bez wymyślania fikcyjnej ceny referencyjnej; brak zmiany w panelu oznacza utrzymanie tej samej ceny.
- W aktywnym przepływie Kwadrans na już rezerwacja po odpowiedzi admina zachowuje usługę priorytetową, właściwą cenę i numer klienta z formularza. Link z e-maila prowadzi do płatności, a po opłaceniu krótkie formaty pozostają telefoniczne.
- Potwierdzenie pełnej konsultacji dostaje dodatkowo jednorazowy link do ustawienia hasła do pokoju klienta; komunikacja w pokoju jest opisana jako aktywna przez 14 dni. Nazwa operatora nie występuje w komunikacji publicznej ani klienckiej.
- Usunięto lokalne rekordy testowych rezerwacji z `data/bookings.json` i `data/lead-bookings.json`; dane produkcyjne pozostają poza repozytorium. Wygenerowane artefakty Stage 9 są ignorowane przez Git.
- Walidacja po zmianach: `npx.cmd tsc --noEmit --incremental false` — OK; `npm.cmd test` — 86 zaliczonych, 0 niezaliczonych, 13 pominiętych; `npm.cmd run lint` — OK; `npm.cmd run schema-audit` — OK; `npm.cmd run build` — OK, 92/92 stron statycznych.
- Commit, deploy i ponowna kontrola w Search Console pozostają osobną bramką release. Po deployu trzeba ponowić sprawdzenie adresu głównego i raportu fragmentów opinii w GSC.

### 2026-07-14 — release produkcyjny i kontrola realnego stanu

- Commit `3b4d613` został wdrożony produkcyjnie na `https://regulskibehawiorysta.pl`.
- `npm run release-checklist`: 18/18 kontroli i 5/5 reguł smoke — OK.
- `npm run live-smoke`: 5/5 tras — OK; produkcja odpowiada markerem `CLEAN_START_REPO_V1:main:3b4d613`.
- Pełny crawl zakończył skan 98/98 adresów ze statusem HTTP 200 i bez awarii crawl; raport zapisał dwa sygnały do interpretacji: oczekiwane 401 dla niezalogowanego `/pokoj` oraz przerwane żądania chunków przy technicznym legacy `/slot`. Nie są to błędy publicznych stron ani blokery indeksowania.
- Wariant crawlera z ponownym odkrywaniem linków przekracza limit czasu narzędzia, dlatego nie traktuję timeoutu jako sukcesu; wiarygodnym artefaktem jest ukończony manifest 98/98 z raportu `qa-reports/final-crawl/report.md`.
- Search Console: własność i sitemap były zweryfikowane wcześniej; po tym deployu pozostaje ręczne odświeżenie inspekcji URL i raportu fragmentów opinii po ponownym crawlu Google.

## Kryteria zakończenia punktów 1–5

1. Jedno źródło cen; brak starych cen w publicznym UI, API, metadata i JSON-LD.
2. Schema opinii nie jest globalnym duplikatem i przechodzi walidację po wdrożeniu.
3. Linki wewnętrzne prowadzą bezpośrednio do stron docelowych.
4. Każdy format ma jednoznaczny kanał i ten sam SLA w całej stronie.
5. Sitemap podaje rzeczywiste, stabilne `lastModified`.
